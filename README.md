# LSwitch - Layout Switcher для Linux

Аналог Caramba Switcher для Linux - быстрый переключатель раскладки клавиатуры с модульной архитектурой.

## Возможности

- ✅ **Конвертация по двойному Shift** - нажмите Shift дважды быстро
- ✅ Автоматическое переключение раскладки после конвертации
- ✅ Конвертация только последнего набранного слова
- ✅ Работает на уровне ядра (evdev) - **максимальная скорость**
- ✅ Работает везде: X11, Wayland, даже в консоли (tty)
- ✅ Поддержка EN ⟷ RU раскладок
- ✅ **Автозапуск через systemd демон**
- ✅ **GUI с иконкой в трее** - управление через меню
- ✅ **Поддержка множества DE** - Cinnamon, KDE Plasma, и другие
- ✅ **Модульная архитектура** - легко добавить новые DE
- ✅ **Поддержка темной темы** - автоматическая адаптация под DE
- ✅ **Режим автопереключения** - включается через трей

## Поддерживаемые окружения

| Desktop Environment | Статус | Меню | Примечания |
|---------------------|--------|------|------------|
| **Cinnamon** | ✅ Полная | CustomMenu | Linux Mint |
| **KDE Plasma** | ✅ Полная | QMenu | X11 + Wayland |
| **GNOME Shell** | ⏳ В разработке | - | Планируется |
| **XFCE** | ⏳ В разработке | - | Планируется |
| Другие | ⚠️ Generic | CustomMenu | Fallback режим |

## Структура проекта

```
LSwitch/
├── lswitch.py              # Основной демон
├── lswitch_control.py      # Панель управления
├── lswitch_tray.py         # Системный трей
├── dictionary.py           # Словарь для автопереключения
├── ngrams.py              # N-грамм анализ
├── user_dictionary.py      # Пользовательский словарь
│
├── adapters/              # GUI адаптеры для DE
│   ├── kde.py            # KDE Plasma
│   ├── cinnamon.py       # Linux Mint Cinnamon
│   └── base.py           # Базовый класс
│
├── utils/                 # Утилиты
│   ├── desktop.py        # Определение DE
│   └── theme.py          # Работа с темами
│
├── drivers/              # Драйверы display серверов
├── docs/                 # Документация
├── scripts/              # Скрипты установки/диагностики
├── config/               # Конфигурационные файлы
└── tests/                # Тесты
```

📖 Подробная архитектура: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

## Режимы работы

### 1. GUI режим с иконкой в трее (рекомендуется)

Удобный интерфейс с системным треем:
- Иконка в системном трее
- Меню по правой кнопке мыши (адаптируется под DE)
- Управление службой (запуск/остановка/перезапуск)
- Чекбоксы настроек:
  - Автопереключение
  - Самообучающийся словарь
  - Автозапуск службы
- Просмотр логов
- Информация о программе
- Автоматический запуск при входе в систему

### 2. Systemd демон

Работает в фоне без GUI - подходит для серверов или минимальных установок.

## Быстрая установка

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/lswitch.git
cd lswitch

# Установите в систему (потребуются права root)
sudo ./install.sh
```

Скрипт установки автоматически:
- Установит необходимые зависимости (python3-evdev, python3-pyqt5)
- Скопирует файлы в систему
- Настроит GUI с автозапуском и systemd демон
- **Настроит права доступа к устройствам ввода через udev**
- **Добавит вашего пользователя в группу `input`**
- Определит ваше DE и применит подходящий адаптер
- Предложит выбрать режим работы

⚠️ **ВАЖНО:** После установки **перелогиньтесь** (выйдите и войдите снова) для применения прав доступа!

Без перелогина программа будет требовать sudo. После перелогина работает без root.

### Почему нужны особые права?

LSwitch перехватывает события клавиатуры на уровне ядра через `/dev/input/`.  
По умолчанию доступ к этим устройствам есть только у root.

**Решение:** Скрипт установки добавляет вас в группу `input` и настраивает udev правила.  
Это **безопасный** и **стандартный** способ, используемый многими Linux-приложениями.

📖 Подробнее: [PERMISSIONS.md](PERMISSIONS.md)

## Управление

### GUI режим

После установки запустите:

```bash
lswitch-tray
```

Или найдите в меню приложений: **Приложения → LSwitch**

**Управление через трей:**
- Правый клик по иконке → Меню
- **Автопереключение** - включить/выключить режим автоматического переключения раскладки
- **О программе** - информация о версии
- **Выход** - закрыть приложение

### Systemd демон

После установки используйте стандартные команды systemd:

```bash
# Запустить
sudo systemctl start lswitch

# Остановить
sudo systemctl stop lswitch

# Перезапустить
sudo systemctl restart lswitch

# Статус
sudo systemctl status lswitch

# Включить автозапуск
sudo systemctl enable lswitch

# Отключить автозапуск
sudo systemctl disable lswitch

# Просмотр логов в реальном времени
sudo journalctl -u lswitch -f
```

### Или используйте Makefile

```bash
make install    # Установить
make start      # Запустить
make stop       # Остановить
make restart    # Перезапустить
make status     # Статус
make enable     # Включить автозапуск
make logs       # Просмотр логов
make uninstall  # Удалить из системы
```

## Ручной запуск (без установки)

### Системные зависимости

```bash
sudo apt install python3-evdev python3-pyqt5 xclip xdotool
```

### Запуск GUI версии

```bash
python3 lswitch_tray.py
```

### Запуск консольной версии

⚠️ **Требуются права root** для доступа к `/dev/input/`:

```bash
sudo python3 lswitch.py
```

## Использование

1. Запустите программу
2. Печатайте текст в любом приложении
3. Набрали слово не в той раскладке? Нажмите **Shift дважды** быстро
4. Последнее слово автоматически конвертируется и раскладка переключается! ✨

### Примеры

- `ghbdtn` → `привет` (при двойном Shift + автопереключение раскладки)
- `руддщ цщкдв` → `hello world` (при двойном Shift + автопереключение раскладки)

## Конфигурация

Файл конфигурации после установки находится в `/etc/lswitch/config.json`:

```json
{
  "double_click_timeout": 0.3,
  "debug": false,
  "switch_layout_after_convert": true,
  "layout_switch_key": "Alt_L+Shift_L"
}
```

### Параметры

- `double_click_timeout` - максимальное время между нажатиями Shift (в секундах)
- `debug` - включить отладочные сообщения
- `switch_layout_after_convert` - автоматически переключать раскладку после конвертации
- `layout_switch_key` - комбинация клавиш для переключения раскладки

После изменения конфигурации перезапустите сервис:
```bash
sudo systemctl restart lswitch
```

## Удаление

```bash
sudo ./uninstall.sh
# Или
make uninstall
```
{ от root: `sudo python3 lswitch.py`
2. Печатайте текст в любом приложении
3. Набрали слово не в той раскладке? Нажмите **Shift дважды** быстро
4. Последнее слово автоматически конвертируется и раскладка переключается! ✨

### Примеры

- `ghbdtn` → двойной Shift → `привет` (автопереключение на RU)
- `руддщ` → двойной Shift → `hello` (автопереключение на EN)
- `ntcn cdjql` → пробел разделяет слова → двойной Shift конвертирует только `cdjql` → `свой`

- **double_click_timeout** (0.3) - время между нажатиями Shift в секундах
- **debug** (true/false) - показывать отладочные сообщения
- **switch_layout_after_convert** (true/false) - переключать раскладку после конвертации
- **layout_switch_key** - шорткат переключения раскладки:
  - `Alt_L+Shift_L` (по умолчанию, Alt+Shift)
  - `Ctrl_L+Shift_L` (Ctrl+Shift)
  - `Super_L+space` (Win+Space)
  - Или ваш собственный шорткат

## Документация

- 📖 [Быстрый старт](docs/QUICKSTART.md) - начало работы за 5 минут
- 📦 [Установка](docs/INSTALL.md) - детальная инструкция по установке
- 🏗️ [Архитектура](docs/ARCHITECTURE.md) - техническая документация
- 💡 [Примеры](docs/EXAMPLES.md) - примеры использования
- 🖥️ [GUI и автопереключение](docs/GUI_AUTOSWITCH.md) - работа с интерфейсом
- 🚀 [Деплой](docs/DEPLOYMENT.md) - развёртывание в продакшене
- 🔧 [Решение проблем](docs/TROUBLESHOOTING.md) - FAQ и типичные проблемы
- 🔑 [Права доступа](docs/PERMISSIONS.md) - настройка прав для evdev
- 🧪 [Тестирование](docs/TESTING.md) - как запускать модульные и GUI тесты локально и в CI
- � [Краткая инструкция по структуре и flow](docs/APP_OVERVIEW.md) - как устроено приложение и как оно работает
- �📝 [История изменений](docs/CHANGELOG.md) - что нового

## Лицензия

MIT License - см. [LICENSE](LICENSE)
 через systemd

Создайте файл `/etc/systemd/system/lswitch.service`:

```ini
[Unit]
Description=LSwitch Keyboard Layout Switcher
After=multi-user.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /полный/путь/к/lswitch.py
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl enable --now lswitch
sudo systemctl status lswitch
```

## Требования

- Linux (любой дистрибутив с evdev)
- Python 3.6+
- python3-evdev
- Права root

## Технические детали

- Работает на уровне `/dev/input/` через evdev
- Создаёт виртуальную клавиатуру для эмуляции событий
- Фильтрует собственные события по имени устройства (нет зацикливания)
- Буфер сбрасывается при пробеле/Enter (конвертирует только последнее слово)ol
- xclip

## Лицензия

MIT
