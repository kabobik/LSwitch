# LSwitch - Changelog

## [Unreleased] - 2026-01-15

### 🧭 Рефакторинг: Упрощение модели конфигурации и GUI

- **Изменено**: удалена административная панель и связанные опции из GUI — теперь GUI управляет исключительно пользовательским конфигом (`~/.config/lswitch/config.json`).
- **Изменено**: удалена поддержка и попытки применить глобальный конфиг `/etc/lswitch/config.json`; приложение создаёт пользовательский конфиг, если тот отсутствует.
- **Безопасность**: админская логика и операции с устройствами были убраны из GUI, чтобы исключить захват `/dev/input` и незапланированные перезапуски демона.
- **Тесты**: удалены/переработаны тесты, связанные с admin-mode; добавлены тесты для user-only config и безопасного сохранения конфига.

### 🐛 Исправление: Стабильность тестов в headless CI

- **Добавлено**: поддержка переменной окружения `LSWITCH_TEST_DISABLE_MONITORS`, отключающей запуск нативных мониторинговых потоков в тестовой среде.
- **Изменено**: тесты теперь устанавливают `QT_QPA_PLATFORM=offscreen` и удаляют `DBUS_SESSION_BUS_ADDRESS` в `tests/conftest.py`, чтобы избежать загрузки XCB/DBus плагинов в headless CI.
- **Добавлено**: `tests/test_monitor_disable.py` — юнит-тесты, проверяющие поведение мониторинга в тестовом режиме и в нормальном режиме.

### 🎉 Крупное обновление: Модульная архитектура

#### ✨ Добавлено

**Новая модульная архитектура:**
- Создана структура `utils/` для утилит определения среды и тем
- Создана структура `adapters/` для GUI адаптеров под разные DE
- Создана структура `drivers/` для будущих драйверов display серверов

**Утилиты (utils/):**
- `utils/desktop.py` - определение Desktop Environment и Display Server
  - `detect_desktop_environment()` - определяет Cinnamon, KDE, GNOME, XFCE, MATE
  - `detect_display_server()` - определяет X11 или Wayland
  - `get_environment_info()` - полная информация об окружении

- `utils/theme.py` - чтение цветов тем из разных DE
  - `get_cinnamon_theme_colors()` - парсит GTK CSS файлы
  - `get_kde_theme_colors()` - читает ~/.config/kdeglobals
  - `get_theme_colors(de_name)` - универсальный интерфейс
  - `get_default_dark_colors()` - fallback цвета

**Адаптеры (adapters/):**
- `adapters/base.py` - абстрактный базовый класс BaseGUIAdapter
  - `create_menu(parent)` - создание меню
  - `get_theme_colors()` - получение цветов темы
  - `supports_native_menu()` - проверка поддержки нативного QMenu

- `adapters/cinnamon.py` - адаптер для Cinnamon
  - `CustomMenuItem` - кастомный пункт меню (48px, 24px шрифт)
  - `CustomMenuSeparator` - разделитель
  - `CustomMenu` - всплывающее меню (QWidget)
  - `QMenuWrapper` - обертка для совместимости с QMenu API
  - `CinnamonAdapter` - главный класс адаптера

- `adapters/kde.py` - адаптер для KDE Plasma
  - Использует нативное QMenu
  - Применяет QPalette из kdeglobals
  - Полная интеграция с темами Breeze/Breeze Dark
  - `KDEAdapter` - главный класс адаптера

- `adapters/__init__.py` - фабрика адаптеров
  - `get_adapter()` - автоматический выбор адаптера по DE

**GUI:**
- Новый `lswitch_control.py` (модульная версия)
  - Использует адаптеры через `get_adapter()`
  - Единый код для всех DE
  - Автоматическое определение окружения
  - Показывает информацию о DE и адаптере в "О программе"

**Тестирование:**
- `test_adapters.py` - скрипт для тестирования адаптеров
  - Проверка определения DE
  - Проверка чтения цветов темы
  - Проверка создания меню
  - Проверка совместимости API

**Документация:**
- `ARCHITECTURE.md` - полное описание архитектуры
  - Обзор модулей
  - API документация
  - Инструкция по добавлению новых DE
  - Технические детали и обоснования решений
  - Roadmap развития

- Обновлён `README.md`
  - Таблица поддерживаемых DE
  - Ссылка на ARCHITECTURE.md
  - Информация о модульной архитектуре

#### 🔧 Изменено

- Рефакторинг `lswitch_control.py`:
  - Удалены встроенные CustomMenu классы (вынесены в adapters/cinnamon.py)
  - Удалена функция `get_system_theme_colors()` (заменена на utils/theme.py)
  - Упрощён код - меню создаётся через адаптеры
  - Добавлена поддержка как QMenu, так и CustomMenu

- Старая версия сохранена как `lswitch_control_old.py`

#### 🎯 Улучшено

**Расширяемость:**
- Легко добавить поддержку новых DE (3 шага + 1 файл)
- Чёткое разделение ответственности между модулями
- Изолированный код для каждого DE

**Поддержка DE:**
- ✅ Полная поддержка Cinnamon (CustomMenu)
- ✅ Полная поддержка KDE Plasma (нативное QMenu)
- ⏳ Готовность к добавлению GNOME, XFCE, MATE

**Качество кода:**
- Использование абстрактных базовых классов (ABC)
- Type hints где возможно
- Docstrings для всех функций и классов
- Модульность и переиспользование кода

#### 🐛 Исправлено

- Правильная обработка QSystemTrayIcon как parent для меню
  - QMenu создаётся с parent=None вместо QSystemTrayIcon
- Совместимость API между CustomMenu и QMenu
  - QMenuWrapper обеспечивает единый интерфейс
- Корректная синхронизация QAction с CustomMenuItem
  - toggled и changed signals правильно связаны

#### 📊 Статистика

**Добавлено файлов:** 8
- 3 файла в utils/ (~6.7 KB)
- 4 файла в adapters/ (~15.0 KB)
- 1 test файл (~1.7 KB)

**Строк кода:**
- utils/desktop.py: ~80 строк
- utils/theme.py: ~160 строк
- adapters/base.py: ~50 строк
- adapters/cinnamon.py: ~220 строк
- adapters/kde.py: ~110 строк
- lswitch_control.py: ~400 строк (было ~896)

**Сокращение дублирования:**
- Код определения DE: было в main(), стало в utils/desktop.py
- Код чтения темы: было в get_system_theme_colors(), стало в utils/theme.py
- CustomMenu классы: было встроено, стало в adapters/cinnamon.py

---

## [1.0.0] - 2026-01-08

### ✨ Первый релиз

#### Основные возможности:
- Конвертация текста по двойному Shift
- Автопереключение раскладки
- Работа через evdev (максимальная скорость)
- Поддержка X11
- Systemd демон
- GUI с иконкой в трее
- Поддержка темной темы для Cinnamon
- CustomMenu для обхода проблем Cinnamon с QMenu

#### Известные ограничения:
- Работает только на Cinnamon
- Только X11 (нет Wayland)
- Захардкоженные цвета темы Mint-Y-Dark-Aqua

---

## Легенда символов

- ✨ Добавлено (Added)
- 🔧 Изменено (Changed)
- 🐛 Исправлено (Fixed)
- 🎯 Улучшено (Improved)
- ⚠️ Устарело (Deprecated)
- ❌ Удалено (Removed)
- 🔒 Безопасность (Security)
- 📊 Статистика (Statistics)
