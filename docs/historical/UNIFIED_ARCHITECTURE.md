# LSwitch - Унифицированная архитектура

## 🎯 Концепция: GUI управляет службой

Теперь LSwitch работает по правильной архитектуре:
- **Одна служба** (systemd) - делает всю работу
- **GUI панель** (опционально) - только управление

### Преимущества

✅ **Нет дублирования процессов** - только один экземпляр lswitch.py  
✅ **Служба работает независимо** - даже если GUI закрыт  
✅ **GUI без root** - управление через pkexec/polkit  
✅ **Живая перезагрузка конфига** - SIGHUP без перезапуска  
✅ **Можно использовать оба режима** - GUI + служба одновременно  

## 📋 Компоненты

### 1. Основная служба (lswitch.py)
```
/usr/local/bin/lswitch
systemd: lswitch.service
```

**Функции:**
- Перехват клавиатуры через evdev
- Конвертация текста
- Автопереключение
- Обработка SIGHUP для перезагрузки конфига

### 2. Панель управления (lswitch_control.py)
```
/usr/local/bin/lswitch-control
```

**Функции:**
- Иконка в системном трее
- Управление службой (start/stop/restart)
- Включение/выключение автопереключения
- Управление автозапуском
- Просмотр логов
- Не запускает собственный процесс!

## 🚀 Установка

```bash
sudo ./install.sh
```

Скрипт установит:
1. Службу lswitch.service
2. Панель управления lswitch-control
3. Оба .desktop файла (старый lswitch-tray и новый lswitch-control)

## 🎮 Использование

### Вариант 1: Только служба (серверы)

```bash
sudo systemctl start lswitch
sudo systemctl enable lswitch
```

Управление через systemctl, настройка через `/etc/lswitch/config.json`

### Вариант 2: Служба + GUI панель (desktop)

```bash
# Служба запускается автоматически
sudo systemctl enable lswitch
sudo systemctl start lswitch

# GUI панель в автозапуске
mkdir -p ~/.config/autostart
cp lswitch-control.desktop ~/.config/autostart/

# Или запустить вручную
lswitch-control
```

**Что происходит:**
1. Служба работает в фоне (от systemd).
2. GUI показывает статус и управляет службой.
3. По умолчанию конфигурация — пользовательская: `~/.config/lswitch/config.json`.
   - Инсталлятор создаёт локальный конфиг для пользователя (если его нет).
   - Системный конфиг `/etc/lswitch/config.json` поддерживается для административного контроля.
   - В системном конфиге можно указать `"allow_user_overrides": false`, чтобы запретить использование локальных конфигов.
4. При изменении настроек GUI:
   - Сохраняет локальный `~/.config/lswitch/config.json` без лишних запросов — это комфортно для пользователя.
   - Есть опция "Применять изменения глобально по умолчанию": при её включении GUI попытается автоматически применить локальные изменения в `/etc/lswitch/config.json` (используя `pkexec`/`sudo`), если системная политика это позволяет.
   - Также доступна отдельная команда "Применить глобально сейчас" в меню для однократного применения (подойдёт администраторам).
   - Если `/etc/lswitch/config.json` запрещает override (`allow_user_overrides=false`), GUI уведомит пользователя и не будет автоматически перезаписывать системный конфиг. Администратор может использовать админский лаунчер (pkexec) для ручного применения.

## 🔧 Как это работает

### Изменение настроек

**Через GUI:**
```
Пользователь: Включает "Автопереключение"
       ↓
GUI: Меняет config.json (через pkexec если нужно)
       ↓
GUI: посылает SIGHUP службе (рекомендуется): `systemctl --user kill --signal=HUP lswitch` (фолбэк: `pkill -HUP -f lswitch`)
       ↓
Служба: Получает SIGHUP → reload_config()
       ↓
Служба: Работает с новыми настройками БЕЗ перезапуска
```

**Через файл:**
```bash
sudo nano /etc/lswitch/config.json
# Меняете "auto_switch": true

# Перезагрузить конфиг без перезапуска (рекомендуется)
# Для пользовательской службы:
systemctl --user kill --signal=HUP lswitch
# Для системной службы с sudo:
sudo systemctl kill --signal=HUP lswitch

# ИЛИ полный перезапуск
sudo systemctl restart lswitch
```

### Управление службой

GUI использует:
- `systemctl start/stop/restart` - через pkexec
- `systemctl is-active` - проверка статуса
- `systemctl enable/disable` - автозапуск
- `systemctl --user kill --signal=HUP lswitch` (фолбэк: `pkill -HUP -f lswitch`) - перезагрузка конфига

## 📊 Сравнение со старым подходом

| Характеристика | Старый (GUI + процесс) | Новый (GUI управляет службой) |
|---|---|---|
| Процессов lswitch | 2 (GUI + основной) | 1 (только служба) |
| Память | ~50MB | ~20MB + GUI временно |
| GUI закрыли | Служба остановилась ❌ | Служба работает ✅ |
| Обновить настройки | Перезапуск ❌ | SIGHUP ✅ |
| Конфликты | Возможны при двух режимах | Невозможны |

## 🔍 Проверка

```bash
# 1. Служба работает?
systemctl status lswitch

# 2. Сколько процессов?
pgrep -af lswitch
# Должен быть только ОДИН: /usr/local/bin/lswitch

# 3. GUI работает?
pgrep -af lswitch-control
# Если запущен GUI, будет: /usr/local/bin/lswitch-control

# 4. Тест перезагрузки конфига
sudo nano /etc/lswitch/config.json  # Меняем debug: true
sudo pkill -HUP -f lswitch.py       # Отправляем сигнал
sudo journalctl -u lswitch -n 20    # Видим "Конфигурация перезагружена"
```

## ⚙️ Технические детали

### SIGHUP обработка

```python
# lswitch.py
def handle_sighup(signum, frame):
    app.config_reload_requested = True

signal.signal(signal.SIGHUP, handle_sighup)

# В главном цикле
while True:
    if self.config_reload_requested:
        self.reload_config()
    # ... обработка событий
```

### GUI без root прав

```python
# lswitch_control.py
def run_systemctl(self, action):
    # Пробуем через pkexec (запросит пароль в GUI)
    subprocess.run(['pkexec', 'systemctl', action, 'lswitch'])
```

### Разрешения

GUI нужны права только для:
- Управления службой → pkexec systemctl
- Записи в /etc/lswitch/config.json → pkexec cp

Служба работает от пользователя из группы `input`.

## 🎯 Рекомендации

**Для desktop пользователей:**
```bash
# Один раз настроить
sudo systemctl enable lswitch
sudo systemctl start lswitch
cp lswitch-control.desktop ~/.config/autostart/

# Дальше управлять через GUI
# Служба всегда работает, GUI показывает когда нужно
```

**Для серверов:**
```bash
# Только служба
sudo systemctl enable lswitch
sudo systemctl start lswitch

# Настройка через файл
sudo nano /etc/lswitch/config.json
sudo systemctl restart lswitch
```

## 📝 Миграция со старой версии

Если установлен старый lswitch-tray:

```bash
# 1. Остановить старый GUI (он убьет свой процесс)
pkill -f lswitch-tray

# 2. Убрать из автозапуска
rm ~/.config/autostart/lswitch-tray.desktop

# 3. Включить службу
sudo systemctl enable lswitch
sudo systemctl start lswitch

# 4. Установить новую панель
cp lswitch-control.desktop ~/.config/autostart/
lswitch-control
```

Теперь GUI управляет службой, не дублируя процесс!
