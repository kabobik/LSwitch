# Установка LSwitch

## Требования

### Обязательные зависимости

| Компонент | Назначение | Критичность |
|-----------|-----------|-------------|
| Python 3.8+ | Основной интерпретатор | **Критично** |
| pip3 | Установка Python пакетов | **Критично** |
| python3-dev | Компиляция evdev | **Критично** |
| evdev | Чтение событий клавиатуры | **Критично** |
| xclip | Работа с буфером обмена | **Критично** |
| xdotool | Эмуляция клавиш | **Критично** |
| systemd | Управление демоном | Требуется |
| udev | Права доступа к /dev/input | Требуется |

### Опциональные зависимости

| Компонент | Назначение |
|-----------|-----------|
| PyQt5 | GUI панель управления (lswitch-control) |
| python-xlib | Определение раскладки (рекомендуется) |

### Требования к окружению

- **Display Server:** X11 (основной), Wayland (ограниченная поддержка)
- **Desktop Environment:** Любое (GNOME, KDE, XFCE, i3, и др.)
- **Права доступа:** Пользователь должен быть в группе `input`

### Установка зависимостей по дистрибутивам

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install python3 python3-pip python3-dev xclip xdotool python3-pyqt5
```

**Fedora:**
```bash
sudo dnf install python3 python3-pip python3-devel xclip xdotool python3-qt5
```

**Arch Linux:**
```bash
sudo pacman -S python python-pip xclip xdotool python-pyqt5
```

**openSUSE:**
```bash
sudo zypper install python3 python3-pip python3-devel xclip xdotool python3-qt5
```

### Проверка зависимостей

После установки зависимостей можно проверить окружение:

```bash
python3 scripts/check_dependencies.py
```

## Быстрая установка

```bash
git clone https://github.com/kirill-2l/LSwitch.git
cd LSwitch
bash scripts/install.sh
```

Скрипт автоматически:
- Установит системные зависимости (`xclip`, `xdotool`)
- Установит пакет через `pip install -e .`
- Скопирует systemd unit, udev-правила, .desktop файл
- Добавит пользователя в группу `input`
- Предложит включить автозапуск

> **💡 Переустановка/обновление:** Скрипт безопасен для повторного запуска. При обнаружении существующей установки он:
> - Покажет текущую версию
> - Спросит подтверждение на переустановку
> - Остановит демон, обновит файлы, перезапустит сервис
> - Сохранит настройки из `~/.config/lswitch/config.json`

## Ручная установка

```bash
# Зависимости (Ubuntu/Debian)
sudo apt install python3-dev xclip xdotool python3-pyqt5

# Установка пакета с GUI
sudo pip3 install --break-system-packages -e ".[gui]"

# Права доступа к клавиатуре
sudo usermod -a -G input $USER
# ⚠️ Перелогиниться для применения!

# Копирование системных файлов
sudo cp config/lswitch.service /etc/systemd/user/lswitch.service
sudo cp config/99-lswitch.rules /etc/udev/rules.d/99-lswitch.rules
sudo cp config/lswitch-control.desktop /usr/share/applications/
sudo cp config/lswitch-control.desktop /etc/xdg/autostart/
sudo cp assets/lswitch.png /usr/share/pixmaps/
sudo udevadm control --reload-rules
sudo udevadm trigger

# Обновление systemd
systemctl --user daemon-reload

# Запуск
systemctl --user enable --now lswitch
```

> **Примечание:** Для Python 3.12+ требуется флаг `--break-system-packages` из-за PEP 668.

## Управление сервисом

```bash
systemctl --user start lswitch      # Запустить
systemctl --user stop lswitch       # Остановить
systemctl --user restart lswitch    # Перезапустить
systemctl --user status lswitch     # Статус
systemctl --user enable lswitch     # Автозапуск ON
systemctl --user disable lswitch    # Автозапуск OFF
```

Или через Makefile:

```bash
make start / stop / restart / status / enable / disable / logs
```

## Логи

```bash
journalctl --user-unit=lswitch -f          # В реальном времени
journalctl --user-unit=lswitch -n 100      # Последние 100 строк
# Или
make logs
```

## Настройка

Конфигурация: `~/.config/lswitch/config.json` (создаётся автоматически).

Основные параметры:

```json
{
  "double_click_timeout": 0.3,
  "debug": false,
  "switch_layout_after_convert": true,
  "auto_switch": false,
  "auto_switch_threshold": 10
}
```

Изменения вступают в силу после `systemctl --user restart lswitch`.

Также можно менять настройки через GUI: `lswitch-control`.

## Проверка

1. Откройте текстовый редактор
2. Наберите: `ghbdtn`
3. Быстро нажмите Shift дважды
4. Должно получиться: `привет`

## Удаление

```bash
bash scripts/uninstall.sh
# Или
make uninstall
```

## Разработка

```bash
# Запуск из исходников
lswitch --debug

# Тесты
python3 -m pytest tests/ -v
# Или
make test
```

## Устранение проблем

### Пользователь не в группе input

**Проблема:** LSwitch не может читать события клавиатуры

**Проверка:**
```bash
groups | grep input
```

Если `input` не отображается — у вас нет доступа к `/dev/input`.

**Решение:**

1. Добавить себя в группу input:
```bash
sudo usermod -a -G input $USER
```

2. **Обязательно перелогиниться** (logout → login)

3. Проверить снова:
```bash
groups | grep input
```

4. Проверить доступ к устройствам:
```bash
ls -l /dev/input/event* | head -5
```

Должны увидеть `crw-rw---- 1 root input` (группа `input` имеет права `rw`).

**Альтернатива (не рекомендуется):**

Запускать LSwitch с sudo (небезопасно):
```bash
sudo lswitch
```

### PyQt5 не установлен

**Проблема:** `lswitch-control` не запускается

**Решение:**
```bash
# Ubuntu/Debian
sudo apt install python3-pyqt5
# Или через pip
sudo pip3 install PyQt5
```

### python-xlib не найден

**Проблема:** Предупреждения при запуске

**Решение:**
```bash
sudo pip3 install python-xlib
```

### Wayland вместо X11

**Проблема:** Некоторые функции не работают

**Решение:**

1. Переключиться на X11 сессию при логине
2. Или использовать XWayland (частичная поддержка)

Проверка:
```bash
echo $XDG_SESSION_TYPE
```

## См. также

- [QUICKSTART.md](QUICKSTART.md) — краткий старт
- [PERMISSIONS.md](PERMISSIONS.md) — права доступа
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) — устранение проблем
