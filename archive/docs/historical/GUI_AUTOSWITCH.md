# LSwitch - GUI Mode and Auto-Switch Feature

## Новые возможности

### 🎨 GUI с системным треем
- Иконка в системном трее с адаптацией под темную/светлую тему
- Меню по правой кнопке мыши
- Управление автопереключением
- Автозапуск при входе в систему

### 🔄 Автопереключение раскладки
При включении режима "Автопереключение" программа автоматически определяет, 
что вы печатаете на неправильной раскладке и конвертирует слово при нажатии пробела.

**Как это работает:**
1. Включите чекбокс "Автопереключение" в меню трея
2. Печатайте как обычно
3. При нажатии пробела программа проверяет слово по словарю (>1000 частых слов RU+EN)
4. Если слово в неправильной раскладке - автоматически конвертирует его

**Примеры:**
- Печатаете `ghbdtn` → после пробела автоматически → `привет` 
- Печатаете `руддщ` → после пробела автоматически → `hello`
- Печатаете `спасибо` в английской раскладке → автоконвертация

## Запуск GUI

### Первый запуск (для тестирования):
```bash
python3 lswitch_tray.py
```

### После установки:
```bash
lswitch-tray
```

Или найдите в меню приложений: **Приложения → LSwitch**

## Меню трея

- **Автопереключение** ☑️ - включить/выключить автоматическую конвертацию при печати
- **Статус** - показывает состояние службы
- **О программе** - информация о версии
- **Выход** - закрыть приложение

## Установка

```bash
sudo ./install.sh
```

Выберите:
1. **GUI режим** - рекомендуется для desktop окружений
2. **Systemd демон** - для серверов или минимальных установок

## Как работает автопереключение

1. **Словарь частых слов** - >1000 русских и >1000 английских слов
2. **Проверка при пробеле** - каждое слово проверяется по словарю
3. **Автоконвертация** - если раскладка неправильная, слово конвертируется автоматически
4. **Двойной Shift** - всё ещё работает для ручной конвертации

## Комбинация возможностей

Теперь у вас есть **два способа** исправить текст:

### 1. Автоматический (новый)
Включите "Автопереключение" в трее:
- Печатаете `привет` в английской раскладке → `ghbdtn`
- Нажимаете **Пробел**
- Автоматически конвертируется → `привет ` (с пробелом)

### 2. Ручной (классический)
- Набрали слово `ghbdtn` в неправильной раскладке
- Нажмите **Shift дважды** быстро
- Конвертируется → `привет`

## Отладка

Включите debug режим в конфигурации:
```bash
sudo nano /etc/lswitch/config.json
```

```json
{
  "double_click_timeout": 0.3,
  "debug": true,
  "switch_layout_after_convert": true,
  "layout_switch_key": "Alt_L+Shift_L",
  "auto_switch": true
}
```

Смотрите логи:
```bash
sudo journalctl -u lswitch -f
```

Или для GUI версии запускайте из терминала:
```bash
python3 lswitch_tray.py
```

## Требования

- Python 3.6+
- python3-evdev
- python3-pyqt5 (для GUI)
- xclip, xdotool, setxkbmap

Устанавливаются автоматически через `install.sh`

## FAQ

**Q: Автопереключение не работает**
A: Проверьте что:
   1. Файл `dictionary.py` находится рядом с `lswitch.py`
   2. Чекбокс "Автопереключение" включен в меню трея
   3. В `/etc/lswitch/config.json` параметр `"auto_switch": true`

**Q: Иконка в трее не видна на темной теме**
A: Иконка автоматически адаптируется. Попробуйте перезапустить GUI.

**Q: Можно ли использовать оба режима одновременно?**
A: Нет, выберите либо GUI (lswitch-tray), либо демон (systemctl start lswitch).
   GUI сам запускает внутренний процесс lswitch.py.

**Q: Словарь можно расширить?**
A: Да, редактируйте `dictionary.py`, добавляйте слова в `RUSSIAN_WORDS` и `ENGLISH_WORDS`.
